<%= form_with(model: giohang, local: true) do |form| %>
  <% if giohang.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(giohang.errors.count, "error") %> prohibited this giohang from being saved:</h2>

      <ul>
      <% giohang.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
